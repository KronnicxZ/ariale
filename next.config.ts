import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // El indicador flotante de desarrollo tapa la barra inferior en móvil.
  devIndicators: false,
};

export default nextConfig;
